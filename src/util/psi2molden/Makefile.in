srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

CDBG = -g
COPT = 

LDLIBS += $(LAPACK) $(BLAS)

PSILIBS = -lPSI_qt -lPSI_chkpt -lPSI_psio -lPSI_ciomr -lPSI_ipv1

CSRC = \
psi2molden.c \

BINOBJ = $(CSRC:%.c=%.o)
ALLOC = 

include ../MakeRules

ifneq ($(DODEPEND),no)
$(BINOBJ:%.o=%.d): $(DEPENDINCLUDE)
include $(BINOBJ:%.o=%.d)
endif

