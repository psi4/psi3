srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

PSILIBS = libPSI_qt.a libPSI_ciomr.a libPSI_ipv1.a

CSRC = detcasman.c setup_io.c

BINOBJ = $(CSRC:%.c=%.o)

include ../MakeRules

ifneq ($(DODEPEND),no)
$(BINOBJ:%.o=%.d): $(DEPENDINCLUDE)
include $(BINOBJ:%.o=%.d)
endif

install_man:: detcasman.1
	$(MKDIRS) $(mandir)/man1
	$(INSTALL_INCLUDE) $^ $(mandir)/man1
