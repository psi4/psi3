srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

PSILIBS = -lPSI_chkpt -lPSI_file30 -lPSI_ciomr -lPSI_psio -lPSI_ipv1

TRUESRC = \
ugeom.c \

BINOBJ = $(TRUESRC:%.c=%.o)
ALLOC = 

include ../MakeRules

ifneq ($(DODEPEND),no)
$(BINOBJ:%.o=%.d): $(DEPENDINCLUDE)
include $(BINOBJ:%.o=%.d)
endif

#install_man:: cscf.1
#	$(MKDIRS) $(mandir)/man1
#	$(INSTALL_INCLUDE) $^ $(mandir)/man1
