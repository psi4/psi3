#include <stdio.h>
#include <libdpd/dpd.h>
#define EXTERN
#include "globals.h"

double HXY(char *cart, int irrep, params.omega)
{
  double polar=0.0;
  dpdfile2 X1, Y1;
  dpdbuf4 I;
  char lbl[32];

  sprintf(lbl, "X_%1s_IA (%5.3f)", );
  return polar;
}
