srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

LD = $(FC)
LDLIBS += $(BLAS)

PSILIBS = -lPSI_qt -lPSI_file30 -lPSI_iwl -lPSI_ciomr -lPSI_psio -lPSI_ipv1

CSRC = \
cphf.c

BINOBJ = $(CSRC:%.c=%.o)
ALLOC = 

include ../MakeRules

ifneq ($(DODEPEND),no)
$(BINOBJ:%.o=%.d): $(DEPENDINCLUDE)
include $(BINOBJ:%.o=%.d)
endif
