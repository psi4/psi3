srcdir = @srcdir@
VPATH = @srcdir@

include ../MakeVars

LD = $(CXX)
LDLIBS += $(LAPACK) $(BLAS)
#LDLIBS += /home/users/rking/dmalloc-4.8.2/dmallocc.o
#LDLIBS += /home/users/rking/dmalloc-4.8.2/libdmallocxx.a

PSILIBS = -lPSI_qt -lPSI_chkpt -lPSI_psio -lPSI_ciomr -lPSI_ipv1

CSRC = char_table.c get_optinfo.c misc.c zmat_to_intco.c

CXXSRC = opt.cc compute_B.cc compute_G.cc compute_H.cc compute_q.cc \
         new_geom.cc empirical_H.cc salc.cc internals.cc cartesians.cc \
         delocalize.cc irrep.cc get_syminfo.cc \
         opt_step.cc disp_user.cc disp_all.cc \
         grad_save.cc freq_grad.cc grad_energy.cc energy_save.cc

BINOBJ = $(CSRC:%.c=%.o) $(CXXSRC:%.cc=%.o)
ALLOC = 

include ../MakeRules

ifneq ($(DODEPEND),no)
$(BINOBJ:%.o=%.d): $(DEPENDINCLUDE)
include $(BINOBJ:%.o=%.d)
endif

install_man:: $(TARGET).1
	$(MKDIRS) $(mandir)/man1
	$(INSTALL_INCLUDE) $^ $(mandir)/man1

