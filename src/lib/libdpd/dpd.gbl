#include "dpd.h"

#ifdef EXTERN
#undef EXTERN
#define EXTERN extern
#else
#define EXTERN
#endif

EXTERN int dpd_default;
EXTERN dpd_data dpd_list[2];  /* Just allow two active DPDs for now */
EXTERN dpd_gbl dpd_main;     /* Central cache for all DPD's */
